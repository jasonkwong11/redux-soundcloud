import React from 'react'
// use this component to implement static Header,
// Footer, Playlist or Player components
function App({ children }) {
  return <div>{children}</div>;
}

export default App;
